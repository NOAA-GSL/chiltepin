# SPDX-License-Identifier: Apache-2.0

# User configuration template for MPAS multi-agent forecast workflow
#
# Copy this file to user_config.yaml and customize for your experiment:
#   cp config/user_config.yaml.template config/user_config.yaml
#
# This file overrides default_config.yaml and platform-specific configs.
# Only specify settings you want to change from the defaults.

# =============================================================================
# REQUIRED: Paths
# =============================================================================

# Experiment directory (customize for your system)
experiment_dir: /home/admin/experiments/mpas_forecast/local

workflow_config:
  agent-executor:
    provider: localhost
    max_blocks: 1
    max_workers_per_node: 4
    environment:
      - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas

agents:
  - name: mesh-foo
    type: mesh
    work_dir: /home/admin/experiments/mpas_forecast/site_foo
    executor: agent-executor
    workflow_config:
      service:
        provider: slurm
        partition: slurmpar
        nodes_per_block: 1
        walltime: "00:10:00"
        environment:
          # - source /home/admin/chiltepin/examples/mpas/miniforge3/etc/profile.d/conda.sh
          # - conda activate mpas-example
          - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas
      compute:
        provider: slurm
        partition: slurmpar
        nodes_per_block: 1
        walltime: "00:10:00"
        environment:
          - source /usr/lmod/lmod/init/bash
          - module use /opt/spack-stack/envs/unified-env/modules/Core
          - module load stack-gcc
          - module load cmake
          # - source /home/admin/chiltepin/examples/mpas/miniforge3/etc/profile.d/conda.sh
          # - conda activate mpas-example
          - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas

  - name: mpas-foo
    type: mpas
    work_dir: /home/admin/experiments/mpas_forecast/site_foo
    executor: agent-executor
    workflow_config:
      service:
        provider: slurm
        partition: slurmpar
        nodes_per_block: 1
        walltime: "01:00:00"
        environment:
          - source /usr/lmod/lmod/init/bash
          - module use /opt/spack-stack/envs/unified-env/modules/Core
          - module load stack-gcc
          - module load stack-openmpi
          - module load cmake
          - module load parallel-netcdf
          - module load parallelio
          # - source /home/admin/chiltepin/examples/mpas/miniforge3/etc/profile.d/conda.sh
          # - conda activate mpas-example
          - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas
      compute:
        provider: slurm
        partition: slurmpar
        nodes_per_block: 1
        walltime: "00:10:00"
        environment:
          - source /usr/lmod/lmod/init/bash
          - module use /opt/spack-stack/envs/unified-env/modules/Core
          - module load stack-gcc
          - module load stack-openmpi
          - module load cmake
          - module load parallel-netcdf
          - module load parallelio
          # - source /home/admin/chiltepin/examples/mpas/miniforge3/etc/profile.d/conda.sh
          # - conda activate mpas-example
          - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas
      mpi:
        mpi: true
        provider: slurm
        partition: slurmpar
        nodes_per_block: 1
        walltime: "00:30:00"
        environment:
          - source /usr/lmod/lmod/init/bash
          - module use /opt/spack-stack/envs/unified-env/modules/Core
          - module load stack-gcc
          - module load stack-openmpi
          - module load cmake
          - module load parallel-netcdf
          - module load parallelio
          - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas

  # - name: mesh-bar
  #   type: mesh
  #   work_dir: /home/admin/experiments/mpas_forecast/site_bar
  #   executor: agent-executor
  #   workflow_config:
  #     service:
  #       provider: slurm
  #       partition: slurmpar
  #       nodes_per_block: 1
  #       walltime: "00:10:00"
  #       environment:
  #         # - source /home/admin/chiltepin/examples/mpas/miniforge3/etc/profile.d/conda.sh
  #         # - conda activate mpas-example
  #         - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas
  #     compute:
  #       provider: slurm
  #       partition: slurmpar
  #       nodes_per_block: 1
  #       walltime: "00:10:00"
  #       environment:
  #         - source /usr/lmod/lmod/init/bash
  #         - module use /opt/spack-stack/envs/unified-env/modules/Core
  #         - module load stack-gcc
  #         - module load cmake
  #         # - source /home/admin/chiltepin/examples/mpas/miniforge3/etc/profile.d/conda.sh
  #         # - conda activate mpas-example
  #         - export PYTHONPATH=$PYTHONPATH:/home/admin/chiltepin/examples/mpas
  

# =============================================================================
# OPTIONAL: Model Configuration
# =============================================================================

# Uncomment and customize as needed
model:
  # Quick LLM test path: uncomment mesh_prompt and llm, then comment out model.mesh below.
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Japan."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Chile."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Tornado Alley."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Boulder, Colorado."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Colorado."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers the intermountain west."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Europe, excluding Russia and Iceland."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers central Europe."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Portugal, UK, Italy, Germany, Austria, and Finland."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers western Europe."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers western and southern Europe."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers Europe excluding eastern Europe."
  # mesh_prompt: "Generate a 15km MPAS mesh that covers mainland Europe and the UK, excluding Russia."
  mesh_prompt: "Generate a 20km MPAS mesh that covers Tuscany."
  # mesh_prompt: "Generate a 15km MPAS HRRR mesh."
  # mesh_prompt: "Generate a 15km MPAS mesh covering Paris, France."
  # mesh_prompt: "Generate a 15km MPAS mesh covering the full 10 day path of Hurricane Katrina."
  # mesh_prompt: "Generate a 15km MPAS mesh covering the full track of Hurricane Katrina from the Bahamas through southern Florida and across the Gulf of Mexico to the Louisiana/Mississippi coast."
  # llm:
  #   # model: qwen2.5:14b
  #   model: gemma3:12b
  #   url: http://localhost:11434/api/chat

  llm:
    model: claude-sonnet-5
    url: https://api.anthropic.com/v1/messages
    api_key_env: ANTHROPIC_API_KEY







  # mesh:
    # name: hexes
    # resolution: 15km
    # Add regional specs for regional meshes
    # regional:  # Leave the section out for global meshes
  #     # Choose either "project_hexes" or "create_region" method
  #     project_hexes:
  #       extent_x_km: 5403.0
  #       extent_y_km: 3183.0
  #       center_lat: 38.5
  #       center_lon: -97.5
  #       rotation_degrees: 20.0 # Birds-eye counter-clockwise
      # create_region:  # Choose one of the types: "polygon", "circle", "ellipse", "channel"
  #       polygon:
  #         point: 38.5, -97.5  # A point inside the polygon
  #         vertices:  # In counter clock-wise order (can be any convex shape)
  #         - 21.03, -122.82
  #         - 47.94, -134.20
  #         - 47.94, -60.80
  #         - 21.03, -72.18
  #       circle:
  #         point: 39.0, -105.5 # Center
  #         radius: 400000  # Meters
        # ellipse:
        #   point: 38.0, 138.0 # Center
        #   semi-major-axis: 1580000  # Meters (oriented N/S before rotation)
        #   semi-minor-axis: 1000000  # Meters (oriented E/W before rotation)
        #   orientation-angle: 45 # Clockwise rotation from north
  #       channel:
  #         upper-lat: 23.43676
  #         lower-lat: -23.43676

#   # Forecast settings
#   forecast_length_hours: 24
#   output_interval_hours: 1
#
#   # MPI configuration
  init_ranks: 1
  forecast_ranks: 48

# =============================================================================
# OPTIONAL: Executor Configuration Overrides
# =============================================================================

# Uncomment to override platform defaults
# workflow:
#   compute-executor:
#     nodes_per_block: 4
#     walltime: "03:00:00"

# =============================================================================
# OPTIONAL: Data Sources
# =============================================================================

# Uncomment to use different data sources
# data_sources:
#   ics_source: GFS
#   lbcs_source: GFS

# =============================================================================
# OPTIONAL: Software Versions
# =============================================================================

# Uncomment to use specific versions
# versions:
#   metis: 5.2.1
#   limited_area: master
#   wps: 4.5
#   mpas: v8.2.0